name: Scheduled Refresh

on:
  workflow_dispatch:
  schedule:
    - cron: "17 8 * * *"

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Placeholder refresh
        run: python ml/src/fleetcast/ingest.py
