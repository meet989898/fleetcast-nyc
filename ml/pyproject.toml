[project]
name = "fleetcast"
version = "0.1.0"
description = "NYC taxi demand forecasting pipeline for FleetCast NYC."
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"
